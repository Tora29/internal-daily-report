# ===================================
# ローカル開発環境用テンプレート
# ===================================
# このファイルを .env にコピーして使用してください:
# 以下の値はdocker-compose.ymlで定義した標準的なローカル開発設定です。
# 個人用の設定が必要な場合は、.envファイルで上書きしてください。
# ===================================

# Database (PostgreSQL)
# ローカル開発環境（Docker）
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/daily_report_dev?schema=public"

# AWS設定（LocalStack用）
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_ENDPOINT_URL=http://localhost:4566

# LocalStack設定
LOCALSTACK_ENDPOINT=http://localhost:4566
USE_LOCALSTACK=true

# Next.js設定
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# AWS Cognito（実AWS環境用、開発時は未設定でOK）
# NEXT_PUBLIC_COGNITO_USER_POOL_ID=
# NEXT_PUBLIC_COGNITO_CLIENT_ID=
# NEXT_PUBLIC_COGNITO_REGION=ap-northeast-1

# AWS S3（実AWS環境用、開発時は未設定でOK）
# AWS_S3_BUCKET_NAME=

# その他（必要に応じて追加）
# NODE_ENV=development
